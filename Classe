class Aluno: _init_(self, nome, matricula, email):
  def __init__

self.nome = nome
self.matricula = matricula
self.email = email

9 cadastro_alunos = [] 
10 opcao= 'S'

while opcao== 'S' or opcao== 's': 
  print('Qual nome do aluno? )
  nome = input()
  print('Qual a matricula dele? ') matricula=input()
  matricula = input()
  16 print('Qual o email dele ') 
  email input() 
  novo_aluno = Aluno (nome, matricula, email) 
  cadastro_alunos.append(novo_aluno)
  print('Deseja cadastrar mais um aluno? [S/N]') 
  opcao=input()

  for Aluno in cadastro_alunos:
    print('Nome', Aluno.nome)
    print('Matriculat Aluno.matricula')
    print('E- mail', Aluno.email')
